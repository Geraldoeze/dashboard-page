import { Box, Typography } from "@mui/material";
import React from "react";

const Chart = () => {
  return (
    <Box
      sx={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "70vh",
      }}
    >
      <Typography variant="h3">Currently working on the page</Typography>
    </Box>
  );
};

export default Chart;
